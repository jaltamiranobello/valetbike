<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "custom", media: "all", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link href="https://fonts.googleapis.com/css?family=Rubik:400,500,700" rel="stylesheet" type="text/css" />
    <link href="/favicon.ico" rel="icon" type="image/x-icon" />
    <title>ValetBike</title>

    <style>
      /* Footer Styling */
      .footer {
        background-color: #ffccd5; 
        padding: 20px;
        text-align: center;
      }

      .menu {
        list-style: none;
        display: inline-flex;
        gap: 20px;
        padding: 0;
        margin: 0 0 10px;
      }

      .menu__item {
        margin: 0;
      }

      .menu__link {
        text-decoration: none;
        font-size: 1rem;
        font-weight: 500;
        color: #d80555;
        transition: background-color 0.3s ease, color 0.3s ease;
      }

      .menu__link:hover {
        color: #ffffff;
        border-radius: 4px;
        padding: 5px;
      }

      .footer p {
        font-size: 0.9rem;
        color: #333;
        margin: 0;
        font-weight: 300;
      }
    </style>
  </head>
  <body>
    <% if user_signed_in? %>
      <!-- Navbar -->
      <div class="topnav">
        <%= image_tag("bikeLogo.png", alt: "Logo", class: "logo") %>
        <span class="valetbike">VALETBIKE</span>
        <div class="nav-links">
          <%= link_to "Rent a Bike", "/stations/index", class: "rent-a-bike-link" %>
          <%= link_to "Trip History", "/trips/index", class: "rent-a-bike-link" %>
          <div class="dropdown">
            <button class="dropbtn"><%= link_to "My Account", edit_user_registration_path %></button>
            <div class="dropdown-content">
              <a><%= button_to "Logout", destroy_user_session_path, method: :delete %></a>
            </div>
          </div>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <div id="site-container">
      <%= yield %>
    </div>

    <% if user_signed_in? %>
      <!-- Footer -->
      <footer class="footer">
        <ul class="menu">
          <li class="menu__item"><a class="menu__link" href="<%= root_path %>">Home</a></li>
          <li class="menu__item"><a class="menu__link" href="<%= team_path %>">Team</a></li>
          <li class="menu__item"><a class="menu__link" href="mailto:valetbike@contact.com">Contact</a>
          </li>
        </ul>
        <p>&copy;2024 ValetBike | All Rights Reserved</p>
      </footer>
    <% end %>
  </body>
</html>

